generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model ChatBackup {
  id        String   @id @default(cuid())
  data      String   // JSON string of chat history
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
